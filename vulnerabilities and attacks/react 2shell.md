- Node.js → a runtime that lets JavaScript run on the server.
- React → a JavaScript library for building web interfaces.
- Next.js → a framework built around React that can also provide server-side functionality.
- A vulnerability in one of these → only matters if the target actually uses the vulnerable component/version/configuration.

- curl -I http://10.48.176.183:3000
- u will get something like powered by node.js or something related to it.
- now we know one kind of vulnerability like this, or may be search it up on google regarding the version to get the exploit path.
- <technology name> <version> known vulnerabilities
- but having next.js does not necessarily mean that there will be a vulnerability.
- Observe something
      ↓
Identify the technology
      ↓
Identify the version/configuration
      ↓
Research vulnerabilities affecting THAT target
      ↓
Verify the vulnerability conditions
      ↓
Test in the authorized lab
- find the version after that.
- 
